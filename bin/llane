#!/usr/bin/env node

require('commander')
  .version(require('../package').version)
  .usage('<command> [options]')
  .command('init', '初始化到当前项目')
  .command('server', '启动本地项目服务')
  .command('release', '代码发布')
  .command('config', '设置llane')
  .command('cc', '设置ll')
  .parse(process.argv);

